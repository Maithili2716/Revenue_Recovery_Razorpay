"""Adaptive Revenue Recovery Agent — Block 4.

Subpackage containing the agent decision loop:

    Context → Diagnosis → Candidate Generation → Bandit Selection → Decision

This package does NOT execute recovery actions. It only produces a structured
AgentDecision indicating what recovery action should be taken.
"""
